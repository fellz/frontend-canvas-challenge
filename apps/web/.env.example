# Адрес API. По умолчанию http://localhost:4001 — как у бэкенда из этого репозитория.
VITE_API_URL=http://localhost:4001
# Пауза перед каждым запросом в мс — чтобы разглядеть состояния. По умолчанию 0.
# VITE_API_DELAY_MS=1000
